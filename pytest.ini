[pytest]
markers =
    slow: tests that are slow or use external services
    llm: tests that call the LLM API and may incur cost
    columns: gold cases from agent/evaluation/gold/columns requiring strict matches
